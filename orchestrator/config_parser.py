"""Parse and validate configuration from the interface / API."""


def parse_interface_config(raw: dict) -> dict:
    """Normalize interface payload into internal orchestrator config (stub)."""
    return raw
