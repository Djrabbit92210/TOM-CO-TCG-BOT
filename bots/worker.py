"""Main polling / event loop for a single worker (one egress IP / session pool)."""


class Worker:
    """Runs monitoring and triggers purchase flow via site adapters (stub)."""

    def __init__(self) -> None:
        pass
